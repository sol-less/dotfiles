#!/bin/bash

WALL_DIR="$HOME/Pictures/Wallpapers/Neutral"

CWD="$(pwd)"

cd "$WALL_DIR" || exit

IFS=$'\n'

SEL_WALL=$(for a in *.png *.jpg; do echo -en "$a\0icon\x1f$a\n"; done | rofi -dmenu -p "󰬽  " -theme-str 'entry { placeholder: "Neutrals.."; }')

if [ -n "$SEL_WALL" ]; then
  hyprtheme "$SEL_WALL"
fi

cd "$CWD" || exit
